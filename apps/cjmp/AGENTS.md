# AGENTS.md

Before making changes, read and follow `./.ai/guidelines.md`.

Project defaults:

- treat this workspace as a CJMP-SDK consumer project
- use `project.conf` to identify project type and target platform first
- prefer `keels doctor`, `keels devices`, `keels build`, and `keels run`
- shared MCP templates, skills, and rules are available under `$CJMP_SDK_HOME/cjmp-tools/ai/`
